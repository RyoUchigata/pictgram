$("#" + "<%= "topic_comment_#{@topic.id}" %>").html("<%= escape_javascript(render 'topics/comment', topic: @topic) %>");
$("#" + "<%= "comment_count_#{@topic.id}" %>").html(<%= @topic.comments.count %>)

